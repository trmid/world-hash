
// ENS Type:
export type ENS = `${string}.eth`;

/* ========================================================================================================================================================================= */

// World Interface:
export interface World {
  name: string
  timestamp: number
}