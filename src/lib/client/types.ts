
// ENS Type:
export type ENS = `${string}.eth`;

/* ========================================================================================================================================================================= */

// World Interface:
export interface WorldInfo {
  name: string
  timestamp: number
  creator: string
}